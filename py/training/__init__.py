"""Training infrastructure — batch runner, reward shaping, RL adapter.

Optional dependencies: numpy (required), gymnasium + pettingzoo (for env.py only).
The batch runner and reward shaping work standalone with just numpy.
"""
